{
  "name": "pro-web-app-tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "pro-web-app": "^0.0.2",
    "pro-web-common": "^1.0.6",
    "sinon": "^11.1.2",
    "supertest": "^6.1.6"
  },
  "scripts": {
    "build": "npx tsc --skipLibCheck",
    "test": "npx tsc --skipLibCheck && mocha ./build/*.spec.js --timeout 5000"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.1.1"
  }
}
